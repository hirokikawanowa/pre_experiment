# AI補完記録 0610

## 1. 進行状況と決定事項
- location1（和室）のBlenderモデルをUnityにインポートし、動作確認を開始。
- 実験シーンは最終的に1つのシーンに全ロケーションを並べて構成する方針。
- location1のみでVRやインタラクションのテストを進めてOK。
- Blenderで設定した色やマテリアルはUnityで再調整が必要な場合あり。
- 実験周り（カード操作、データ記録、UI等）は未実装。CardControllerから順に実装を進めることを決定。
- 単語カードはPrefab＋スクリプトで自動生成し、40単語から各部屋に2単語をランダム配置する方針。
- 単語と画像のペアはリスト（ScriptableObjectやクラス）で管理。
- ユーザーがカードを配置した順番もリストで記録し、DataLoggerでログ保存する設計。

## 2. 今後の優先順位
1. CardController（単語カードの掴み・離し）
2. DoorController（ドアの開閉）
3. ProgressUIDisplay（進行状況UI）
4. DataLogger（データ記録）
5. 環境音・ライティング等の最適化

## 3. その他
- 実装はAIの指示に従って進める方針。
- 実装例やサンプルコードが必要な場合は随時AIが提示。 